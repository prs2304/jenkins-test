pipeline {
    agent any
    stages {
        stage ('build') {
            steps {
                echo "********script execution*************"
                script {
                    if (course == k8s)
                    println ("welocme to the $course")
                    else
                    println("do learn k8s")
                }
            }
        }
    }
}
